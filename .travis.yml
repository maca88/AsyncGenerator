language: csharp
script:
  - sudo ./build.sh --target=$TARGET --$SCRIPTARGS
os: osx
matrix:
  include:
    - dotnet: 3.1.201
      env: TARGET=TestCore SCRIPTARGS=
   - dotnet: 6.0.100
      env: TARGET=TestCore SCRIPTARGS="netcore=\"net6.0\""
    - mono: latest
      env: TARGET=Test SCRIPTARGS=
git:
  submodules: false
cache:
  directories:
    - Source/packages